// import { parseTiptapHtml } from '@/lib/tiptap'
import { generateJSON } from '@tiptap/html'

// *parseStressJson()
// TODO: Handle multiple paragraphs
describe('parseStressJson()', () => {
  it('can parse trivial json', () => {
    expect([]).toEqual([])
  })

  // it('can parse simple stress json', () => {
  //   const { given, expected } = cases.simpleStressJson
  //   expect(parseStressJson(given)).toEqual(expected)
  // })
})
